// Utility functions for formatting data - will be implemented in task 2.2

export function formatDate(dateString: string): string {
  // Placeholder implementation
  return dateString;
}

export function formatNumber(num: number, decimals: number = 0): string {
  // Placeholder implementation
  return num.toFixed(decimals);
}

export function calculateRecentFormAverage(matches: any[], statKey: string): number {
  // Placeholder implementation
  return 0;
}